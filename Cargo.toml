[package]
name = "actors"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.45.1", features = [
    "signal",
    "rt",
    "sync",
    "tokio-macros",
    "time",
    "macros",
    "rt-multi-thread",
] }
